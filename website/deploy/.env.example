# Copy to .env on the SERVER (chmod 600), do NOT commit the real file.
#
# Token required by /admin/reload-db.  Generate something long & random:
#   python3 -c 'import secrets; print(secrets.token_urlsafe(32))'
ADMIN_TOKEN=replace-me-with-a-long-random-string
